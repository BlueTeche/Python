#turple is a type of data structure. container for store different values.
# , is immutable, measn cant modify or change it its value
#gereral purpose of the turple function is that a data thats never going to change.

coordinates = [(4, 5), (6, 7), (80, 34)]

#coordinates [1] = 10  #------> this wont never change so will gives error



print(coordinates[0]) # will print show (4, 5)
print(coordinates[1]) # will print show (6, 7)


